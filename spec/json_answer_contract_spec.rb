require 'spec_helper'

describe JsonAnswerContract do
  it 'has a version number' do
    expect(JsonAnswerContract::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
